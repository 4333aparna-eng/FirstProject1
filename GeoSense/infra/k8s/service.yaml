apiVersion: v1
kind: Service
metadata:
  name: geosense-service
  labels:
    app: geosense
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: geosense